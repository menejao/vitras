# RefreshToken

## Objetivo
Documentar refresh token.

## Descrição
Token de renovação persistido com hash.

## Campos
`id`, `tokenHash`, `userId`, `sessionId`, `ip`, `expiresAt`, `revokedAt`, `sessionContext`, `createdAt`.
