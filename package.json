{
  "scripts": {
    "build": "cd frontend-react && npm ci --prefer-offline && npm run build",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "axios": "^1.13.6"
  }
}
