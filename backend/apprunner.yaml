version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci --omit=dev
run:
  command: node src/server.js
  network:
    port: 8080
    env: PORT
  env:
    - name: NODE_ENV
      value: production
